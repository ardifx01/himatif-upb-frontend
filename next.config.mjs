/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "api.microlink.io",
      "localhost",
      "aceternity.com",
      "images.unsplash.com",
      "primefaces.org",
    ],
  },
};

export default nextConfig;
